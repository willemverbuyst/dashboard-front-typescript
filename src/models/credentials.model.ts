export type LoginCredentials = {
  email: string;
  password: string;
  status: number;
};

export type SignUpCredentials = {
  name: string;
  email: string;
  password: string;
  status: number;
  teacherId: number;
};
